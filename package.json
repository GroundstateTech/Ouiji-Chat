{
  "name": "ouiji-chat",
  "version": "3.2.0-alpha",
  "description": "Ouiji Chat - modern AIM-style instant messenger with public and independent-server deployment",
  "main": "client/main.js",
  "private": true,
  "scripts": {
    "server": "node server/server.js",
    "client": "electron .",
    "start": "electron .",
    "doctor": "node scripts/doctor.js",
    "check": "node --check server/server.js && node --check server/security.js && node --check client/main.js && node --check client/preload.js && node --check client/index.js && node --check client/chat.js && node --check client/employee-card.js && node --check scripts/doctor.js",
    "test": "node --test tests/*.test.js",
    "verify": "npm run doctor && npm run check && npm test",
    "package-win": "electron-packager . Ouiji-Chat --platform=win32 --arch=x64 --out=artifacts --overwrite --prune=true"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "dependencies": {
    "ws": "^8.21.3"
  },
  "devDependencies": {
    "@electron/packager": "^20.3.0",
    "electron": "^43.4.1"
  },
  "keywords": [
    "groundstate",
    "ouiji",
    "instant-messenger",
    "aim-style",
    "electron",
    "websocket",
    "self-hosted",
    "private-server",
    "lan"
  ],
  "author": "Groundstate Technology LLC"
}
