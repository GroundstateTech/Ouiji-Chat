{
  "name": "ouiji-inhouse",
  "version": "3.2.0-alpha",
  "description": "Ouiji InHouse - private-first internal team communications client",
  "main": "client/main.js",
  "private": true,
  "scripts": {
    "server": "node server/server.js",
    "client": "electron .",
    "start": "electron .",
    "doctor": "node scripts/doctor.js",
    "check": "node --check server/server.js && node --check server/security.js && node --check client/main.js && node --check client/preload.js && node --check client/index.js && node --check client/chat.js && node --check client/employee-card.js && node --check scripts/doctor.js",
    "test": "node --test tests/*.test.js",
    "verify": "npm run doctor && npm run check && npm test"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "dependencies": {
    "ws": "^8.21.3"
  },
  "devDependencies": {
    "electron": "^43.4.0"
  },
  "keywords": [
    "groundstate",
    "ouiji",
    "internal-chat",
    "electron",
    "websocket",
    "lan"
  ],
  "author": "Michael Muha / Groundstate"
}
